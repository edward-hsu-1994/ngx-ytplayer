import { YtPlayerDirective } from './yt-player.directive';

describe('YtPlayerDirective', () => {
  it('should create an instance', () => {
    const directive = new YtPlayerDirective(null);
    expect(directive).toBeTruthy();
  });
});
